import DynamicPage from '@/components/DynamicPage';

export default function ShotgunFieldsPage() {
  return (
    <DynamicPage
      title="Shotgun Fields"
      subtitle="Trap, skeet, and sporting clays"
    />
  );
}
